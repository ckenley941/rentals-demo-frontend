//In future could extend this class to show more information on the rental
export default function RentalInfo({ name }) {
    return (
        <div className="rental-card-side rental-card-content">
        <h4>{name}</h4>
      </div>
    );
    }

    

